# Evaluation

Thư mục chứa:

- `golden-set.csv`: ít nhất 20 case, có case thường, 4 lớp chỗ khó và case hiếm.
- `results.md`: kết quả đầy đủ của từng lượt chạy, kể cả case không đạt.
- Quy tắc chấm phải đủ rõ để người ngoài nhóm chấm ra cùng kết quả.
