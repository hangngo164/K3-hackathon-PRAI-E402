# Cách 2 để nạp key (thay cho .env) — dùng khi deploy Streamlit Community Cloud.
# Copy thành .streamlit/secrets.toml.  KHÔNG commit secrets.toml.

OPENAI_API_KEY = "sk-thay-bang-key-that"
OPENAI_MODEL_FAST = "gpt-4o-mini"
OPENAI_MODEL_MAIN = "gpt-4o"
