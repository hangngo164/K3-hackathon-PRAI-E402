"""Tab Tóm tắt: chọn scope, hiện bullet có neo trang, nút "xem chỗ này".

TODO(CP3). Tính năng F1.1-F1.5 (FEATURE.md §3).
Không tự gọi model — gọi qua core.summarize.

Phải hiện `not_covered` và `confidence` ngay dưới bản tóm tắt (HAX G2/G10):
người dùng cần biết phần nào hệ thống không đọc được.
"""

from __future__ import annotations


def render() -> None:
    raise NotImplementedError("TODO(CP3)")
